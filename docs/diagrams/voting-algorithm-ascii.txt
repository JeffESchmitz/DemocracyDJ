Voting Algorithm Flow
=====================

This documents how votes flow from Guest tap to Host truth to UI update.


HIGH-LEVEL FLOW
===============

  ┌──────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
  │  Guest   │ ───► │   Mesh   │ ───► │   Host   │ ───► │   All    │
  │  Vote    │      │  Network │      │ Process  │      │  Guests  │
  └──────────┘      └──────────┘      └──────────┘      └──────────┘
       │                                    │                 │
       │                                    │                 │
   Optimistic                          Source of          State
   UI update                            Truth            replaced


DETAILED ALGORITHM
==================

STEP 1: Guest Initiates Vote
────────────────────────────

  User taps 👍 on "Master of Puppets"
        │
        ▼
  ┌─────────────────────────────────────────┐
  │  GuestFeature.Action.voteTapped(        │
  │    songID: "song-123"                   │
  │  )                                      │
  └────────────────────┬────────────────────┘
                       │
                       ▼
  ┌─────────────────────────────────────────┐
  │  1. Add "song-123" to pendingVotes      │
  │     (optimistic UI - button disabled)   │
  │                                         │
  │  2. Create GuestIntent.vote(            │
  │       songID: "song-123"                │
  │     )                                   │
  │                                         │
  │  3. Wrap in MeshMessage.intent(...)     │
  │                                         │
  │  4. Send via multipeerClient            │
  └─────────────────────────────────────────┘


STEP 2: Host Receives & Processes
─────────────────────────────────

  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  func processIntent(_ intent: GuestIntent, from peer: Peer)     │
  │                                                                 │
  │  switch intent {                                                │
  │  case .vote(let songID):                                        │
  │      │                                                          │
  │      ▼                                                          │
  │  ┌─────────────────────────────────────────────────────────┐    │
  │  │  guard let index = queue.index(id: songID) else {       │    │
  │  │      return .none  // Song not in queue - ignore        │    │
  │  │  }                                                      │    │
  │  └─────────────────────────────────────────────────────────┘    │
  │      │                                                          │
  │      ▼                                                          │
  │  ┌─────────────────────────────────────────────────────────┐    │
  │  │  // Insert returns (inserted: Bool, memberAfterInsert)  │    │
  │  │  let (isNew, _) = queue[index].voters.insert(peer.id)   │    │
  │  │                                                         │    │
  │  │  // Set handles idempotency automatically!              │    │
  │  │  // If peer already voted, isNew = false, no change     │    │
  │  └─────────────────────────────────────────────────────────┘    │
  │      │                                                          │
  │      ▼                                                          │
  │  ┌─────────────────────────────────────────────────────────┐    │
  │  │  // Re-sort queue by vote count (descending)            │    │
  │  │  queue.sort { $0.voteCount > $1.voteCount }             │    │
  │  └─────────────────────────────────────────────────────────┘    │
  │      │                                                          │
  │      ▼                                                          │
  │  ┌─────────────────────────────────────────────────────────┐    │
  │  │  // Broadcast new truth to all guests                   │    │
  │  │  return .run { send in                                  │    │
  │  │      try await multipeerClient.send(                    │    │
  │  │          .stateUpdate(HostSnapshot(...)),               │    │
  │  │          to: nil  // nil = broadcast to all             │    │
  │  │      )                                                  │    │
  │  │  }                                                      │    │
  │  └─────────────────────────────────────────────────────────┘    │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘


STEP 3: All Guests Receive Update
─────────────────────────────────

  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  case .multipeerEvent(.messageReceived(let msg, _)):            │
  │      if case .stateUpdate(let snapshot) = msg {                 │
  │          │                                                      │
  │          ▼                                                      │
  │      ┌─────────────────────────────────────────────────────┐    │
  │      │  state.hostSnapshot = snapshot  // REPLACE entirely │    │
  │      │  state.pendingVotes = []        // Clear optimistic │    │
  │      └─────────────────────────────────────────────────────┘    │
  │      }                                                          │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘


IDEMPOTENCY GUARANTEE
=====================

  The Set<String> data structure guarantees idempotency:

  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  var voters: Set<String> = ["peer-1", "peer-2"]                 │
  │                                                                 │
  │  voters.insert("peer-2")  // Already exists                     │
  │  // Result: (inserted: false, memberAfterInsert: "peer-2")      │
  │                                                                 │
  │  voters.count == 2  // Unchanged!                               │
  │                                                                 │
  │  No race conditions. No double-counting. No special logic.      │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘


QUEUE SORTING
=============

  After any vote change, queue is re-sorted:

  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  Before:                          After sort:                   │
  │  ────────                         ────────────                  │
  │  1. Song A (2 votes)              1. Song B (4 votes) ▲         │
  │  2. Song B (4 votes)              2. Song C (3 votes) ▲         │
  │  3. Song C (3 votes)              3. Song A (2 votes) ▼         │
  │                                                                 │
  │  Sorting rule: Higher votes = closer to "Now Playing"           │
  │                                                                 │
  │  Tie-breaker: First added wins (stable sort)                    │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘


OPTIMISTIC UI
=============

  Guest shows immediate feedback while waiting for Host confirmation:

  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  Timeline:                                                      │
  │  ─────────                                                      │
  │                                                                 │
  │  T+0ms    User taps vote                                        │
  │           pendingVotes.insert("song-123")                       │
  │           UI: Button disabled, shows "Voted!"                   │
  │                                                                 │
  │  T+50ms   Message sent to Host                                  │
  │                                                                 │
  │  T+80ms   Host processes, broadcasts HostSnapshot               │
  │                                                                 │
  │  T+150ms  Guest receives HostSnapshot                           │
  │           hostSnapshot = newSnapshot                            │
  │           pendingVotes = []  // Clear                           │
  │           UI: Shows actual vote count from truth                │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘


EDGE CASES
==========

  ┌─────────────────────────────────────────────────────────────────┐
  │ Case                        │ Handling                         │
  ├─────────────────────────────┼──────────────────────────────────┤
  │ Vote on non-existent song   │ Ignored (guard fails)            │
  │ Duplicate vote same peer    │ No-op (Set handles it)           │
  │ Vote while disconnected     │ Queued, sent on reconnect        │
  │ Host receives own vote      │ Works fine (Host can vote too!)  │
  │ Vote during song skip       │ Snapshot broadcast resolves      │
  └─────────────────────────────────────────────────────────────────┘
